"""
app/core/__init__.py
=====================
Core architectural building blocks shared by the entire application.

Contents:
    base_model         — Abstract SQLAlchemy base model with audit fields
    base_repository    — Generic repository pattern implementation
    base_service       — Abstract service layer base class
    exceptions         — Custom application exception hierarchy
    context_processors — Jinja2 template context processors
    security           — Role/permission decorators
"""
